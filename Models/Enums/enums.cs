namespace Models.Enums
{
    public enum PolicyType
    {
        AC,
        Pool,
        SPA,
        Parking
    }
    public enum HotelRoomType{
        Single, Double, Triple, Suite,Other
    }
}
